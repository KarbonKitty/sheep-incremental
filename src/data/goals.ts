import { Map, Price } from "../classes/baseClasses";

const goals: Map<Price> = {
    tribal: { bread: 150, meat: 250, beer: 100 },
    copper: { pottery: 200, bread: 200, meat: 300, beer: 150 }
};

export default goals;
